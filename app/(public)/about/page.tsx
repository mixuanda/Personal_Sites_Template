import GlassPanel from "../../../components/glass/GlassPanel";

export default function AboutPage() {
  return (
    <GlassPanel title="About">
      <p>
        Evanalysis is a personal site template with glassmorphism styling, taxonomy
        support, and a private diary area.
      </p>
      <p>
        This setup is ready for a CMS and math rendering pipeline once you plug in
        MDX tooling.
      </p>
    </GlassPanel>
  );
}
